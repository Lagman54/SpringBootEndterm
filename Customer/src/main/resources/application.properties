spring.application.name=Customer
server.port=8888

#kafka
spring.kafka.bootstrap-servers=localhost:9092
app.kafka.order-created-topic=order-created-topic
app.kafka.order-created-consumers=customer-service-group
app.kafka.payment-result-topic=payment-result-topic

# Admin client properties for topic creation
spring.kafka.admin.properties.bootstrap.servers=localhost:9092

#db
spring.datasource.url=jdbc:postgresql://localhost:5432/customer_db
spring.datasource.username=dev_user
spring.datasource.password=secret123
spring.jpa.hibernate.ddl-auto=none

